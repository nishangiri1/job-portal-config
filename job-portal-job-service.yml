# =============================================================================
# Fetched ONLY by job-portal-job-service from Config Server.
# Common settings (eureka, jpa, resilience4j) come from application.yml.
# =============================================================================

server:
  port: 9002

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job_portal_job
    username: postgres
    password: nishan
    driver-class-name: org.postgresql.Driver
